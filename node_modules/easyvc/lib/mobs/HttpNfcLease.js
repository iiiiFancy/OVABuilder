'use strict'

const BaseMob = require('../BaseMob.js')

class HttpNfcLease extends BaseMob {
	async waitReady(timeoutMillis) {
		let svc = await this._service()
		return this.waitState('state', svc.vim.HttpNfcLeaseState.ready.toString(), timeoutMillis)
	}
}


module.exports = HttpNfcLease